Ext.define( 'LPT.model.StatusInfo', {
    extend: 'Ext.data.Model',

    fields: [
        'id', 'maxMemoryHeap', 'usedMemoryHeap', 'maxMemoryNonHeap', 'maxMemoryNonHeap',
        'maxMemoryNonHeap'
    ],

    idProperty: 'id'
} );
